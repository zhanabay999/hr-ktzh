# Быстрый старт HR-KTZH

## 1. Установите зависимости

```bash
npm install
```

## 2. Настройте `.env.local`

Файл `.env.local` уже существует и настроен. Просто добавьте секретный ключ:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

Скопируйте полученный ключ и вставьте в `.env.local` вместо `your-secret-key-here-generate-with-openssl-rand-base64-32`

## 3. Создайте таблицы в базе данных

```bash
npm run db:push
```

## 4. Заполните начальными данными

```bash
npm run db:seed
```

Это создаст:
- **Супер Админ**: табельный `0000001`, пароль `SuperAdmin123!`
- **5 HR Супер Админов**: табельные `0000002-0000006`, пароль `HRSuper123!`

## 5. Запустите проект

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 6. Войдите в систему

Используйте учетные данные Супер Админа:
- **Табельный номер**: `0000001`
- **Пароль**: `SuperAdmin123!`

---

## Что дальше?

После входа вы можете:

1. **Создать пользователей**:
   - Перейдите в "Пользователи" → "Добавить пользователя"
   - Или используйте "Импорт из Excel" для массовой загрузки

2. **Создать курсы** (только HR Супер Админы):
   - Перейдите в "Курсы" → "Создать курс"
   - Добавьте название, описание и содержание

3. **Управлять ролями**:
   - Супер Админ назначает HR Супер админов
   - HR Супер админы назначают HR Центральных админов
   - И так далее по иерархии

---

## Возможные проблемы

### Ошибка при npm install

```bash
rm -rf node_modules package-lock.json
npm install
```

### Ошибка подключения к базе данных

Проверьте `DATABASE_URL` в `.env.local`

### NextAuth ошибка

Убедитесь, что `NEXTAUTH_SECRET` заполнен в `.env.local`

---

## Готово! 🎉

Система полностью настроена и готова к использованию.
