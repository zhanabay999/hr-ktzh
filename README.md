# HR-KTZH - Система управления персоналом

Система управления персоналом с иерархией ролей и управлением курсами.

## Технологии

- **Next.js 16** - React фреймворк
- **TypeScript** - Типизация
- **PostgreSQL** (Neon) - База данных
- **Drizzle ORM** - ORM для работы с БД
- **NextAuth v5** - Аутентификация
- **Tailwind CSS** - Стили
- **Zod** - Валидация данных

## Иерархия ролей

1. **Супер Админ** (1 человек) - Назначает HR Супер админов
2. **HR Супер Админ** (5 человек) - Назначает HR Центральных админов, **создает курсы**
3. **HR Центральный Админ** - Назначает HR Региональных админов, управляет сотрудниками
4. **HR Региональный Админ** - Назначает HR Линейных админов, управляет сотрудниками
5. **HR Линейный Админ** - Управляет сотрудниками
6. **Сотрудник** - Проходит курсы

## Установка

### 1. Установите зависимости

\`\`\`bash
npm install
\`\`\`

### 2. Настройте переменные окружения

Создайте файл `.env.local` на основе `.env.example`:

\`\`\`bash
cp .env.example .env.local
\`\`\`

Заполните переменные:

\`\`\`env
# Database - получите на neon.tech
DATABASE_URL=postgresql://username:password@host/database?sslmode=require

# NextAuth - сгенерируйте секретный ключ
NEXTAUTH_SECRET=your-secret-key-here
NEXTAUTH_URL=http://localhost:3000
\`\`\`

Для генерации `NEXTAUTH_SECRET`:
\`\`\`bash
openssl rand -base64 32
\`\`\`

### 3. Создайте таблицы в базе данных

\`\`\`bash
npm run db:push
\`\`\`

### 4. Заполните базу начальными данными

\`\`\`bash
npm run db:seed
\`\`\`

Это создаст:
- 1 Супер Админа (табельный: `0000001`, пароль: `SuperAdmin123!`)
- 5 HR Супер Админов (табельные: `0000002-0000006`, пароль: `HRSuper123!`)

### 5. Запустите проект

\`\`\`bash
npm run dev
\`\`\`

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Учетные данные для входа

**Супер Админ:**
- Табельный номер: `0000001`
- Пароль: `SuperAdmin123!`

**HR Супер Админы:**
- Табельные номера: `0000002`, `0000003`, `0000004`, `0000005`, `0000006`
- Пароль: `HRSuper123!`

## Основные возможности

### ✅ Реализовано

- ✅ Аутентификация через табельный номер (7 цифр)
- ✅ 6-уровневая иерархия ролей
- ✅ Защита роутов (middleware)
- ✅ Дашборд с ролевыми правами
- ✅ Базовая навигация

### 🚧 В процессе

- 🚧 Управление пользователями (создание, редактирование, список)
- 🚧 Управление курсами (только HR Супер Админы)
- 🚧 Импорт пользователей из Excel
- 🚧 Назначение ролей по иерархии

### 📋 Запланировано

- 📋 Система курсов и обучения
- 📋 Записи на курсы
- 📋 Прогресс прохождения курсов
- 📋 Почтовый агрегатор (интеграция)
- 📋 Уведомления
- 📋 Отчеты и аналитика

## Структура проекта

\`\`\`
HR-KTZH/
├── app/                      # Next.js App Router
│   ├── api/                  # API routes
│   │   └── auth/            # NextAuth endpoints
│   ├── dashboard/           # Dashboard pages
│   ├── login/               # Login page
│   ├── globals.css          # Global styles
│   ├── layout.tsx           # Root layout
│   └── page.tsx             # Home page
├── components/              # React components
│   ├── auth/               # Auth components
│   ├── dashboard/          # Dashboard components
│   └── ui/                 # UI components
├── db/                     # Database
│   ├── schema.ts          # Drizzle schema
│   ├── index.ts           # DB connection
│   └── seed.ts            # Seed script
├── lib/                   # Utilities
│   ├── auth.ts           # NextAuth config
│   ├── permissions.ts    # Role permissions
│   └── validations.ts    # Zod schemas
├── middleware.ts         # Route protection
└── package.json         # Dependencies
\`\`\`

## Скрипты

\`\`\`bash
npm run dev          # Запуск в режиме разработки
npm run build        # Сборка для production
npm run start        # Запуск production сборки
npm run lint         # Проверка кода
npm run db:generate  # Генерация миграций
npm run db:migrate   # Применение миграций
npm run db:push      # Push schema to database
npm run db:seed      # Заполнение начальными данными
\`\`\`

## Матрица прав доступа

| Роль | Назначает | Создает курсы | Создает сотрудников | Импорт Excel |
|------|-----------|---------------|---------------------|--------------|
| Супер Админ | HR Супер | Нет | Да | Да |
| HR Супер | HR Центральный | **ДА** | Да | Да |
| HR Центральный | HR Региональный | Нет | Да | Да |
| HR Региональный | HR Линейный | Нет | Да | Да |
| HR Линейный | Никого | Нет | Да | Да |
| Сотрудник | Никого | Нет | Нет | Нет |

## Формат импорта Excel

Файл должен содержать следующие колонки:
- `employeeId` - 7-значный табельный номер
- `firstName` - Имя
- `lastName` - Фамилия
- `email` - Email (необязательно)
- `password` - Пароль (минимум 8 символов)
- `role` - Роль (`hr_super`, `hr_central`, `hr_regional`, `hr_line`, `employee`)

## База данных

### Таблицы

**users** - Пользователи
- id (UUID)
- employeeId (7 цифр, уникальный)
- password (хеш)
- firstName, lastName
- email
- role (enum)
- createdBy, isActive
- createdAt, updatedAt

**courses** - Курсы
- id (UUID)
- title, description, content
- createdBy
- isActive
- createdAt, updatedAt

**enrollments** - Записи на курсы
- id (UUID)
- userId, courseId
- status (enum)
- enrolledAt, completedAt

## Безопасность

- ✅ Пароли хешируются с bcrypt (12 раундов)
- ✅ Защита от CSRF через NextAuth
- ✅ SQL injection защита через Drizzle ORM
- ✅ Валидация входных данных с Zod
- ✅ Защита роутов через middleware
- ✅ JWT токены для сессий
- ✅ Проверка прав доступа на основе ролей

## Поддержка

Для вопросов и предложений создавайте issues в репозитории.
\`\`\`
